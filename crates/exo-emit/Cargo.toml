[package]
name = "exo-emit"
version = "0.1.0"
edition = "2021"

[lib]
path = "src/lib.rs"

[features]
default = ["std"]
std = ["exo-ir/std"]

[dependencies]
exo-ir = { path = "../exo-ir", default-features = false, features = ["std", "profile-rust", "profile-logos", "debug-symbols"] }
